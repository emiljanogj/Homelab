apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: plex
  name: movies-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi  # Adjust size as needed
